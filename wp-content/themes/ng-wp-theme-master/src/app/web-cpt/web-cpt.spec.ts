/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {WebCpt} from './web-cpt';

describe('WebCpt', () => {
  it('should create an instance', () => {
    expect(new WebCpt()).toBeTruthy();
  });
});
